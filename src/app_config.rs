pub struct AppConfig {
    pub gk_path: String,      // global key path
    pub auth_dir: String,     // authority key directory
    pub user_dir: String,     // user key directory
    pub storage_path: String, // storage path
}
